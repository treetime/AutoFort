package autofort.abstractions.model.aesthetics.arrangement.placeables

import autofort.abstractions.model.aesthetics.architecture.RoomNeed
import autofort.abstractions.model.aesthetics.arrangement.Placeable
import autofort.abstractions.model.aesthetics.arrangement.Placeable.Dimensions

object Furniture {

  case class Throne() extends Placeable()
  case class Table() extends Placeable()
  case class Statue() extends Placeable()
  case class Bed() extends Placeable()
  case class Chest() extends Placeable()
  case class Bookcase() extends Placeable()
  case class Altar() extends Placeable()
  case class Cabinet() extends Placeable()
  case class Bag() extends Placeable()
  case class TractionBench() extends Placeable()

}
